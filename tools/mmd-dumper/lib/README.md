# Local SDK Libraries

Place local MMD SDK and MMDPlugin developer files here when building native adapters.

Suggested layout:

```text
lib/
  mmd/
    MMDExport.lib
  mmdplugin/
    mmd_plugin.h
    MMDPlugin.lib
```

Binary SDK files are intentionally ignored by git.
